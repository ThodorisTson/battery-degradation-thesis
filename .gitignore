# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Credentials - never commit
.env
token_*
*.key
gurobi.lic

# Large raw data
*.nc

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# Note: results/ is tracked, not ignored. The simulation runs are frozen and a
# full sweep takes hours, so the outputs the thesis figures read from are
# committed. Check `git status` before committing after a new run.
